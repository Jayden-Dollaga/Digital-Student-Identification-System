pyserial==3.5
cryptography>=42.0.0
customtkinter==6.0.0
openpyxl==3.1.5
# Matplotlib is used by the reporting/chart functions in `python/core/database.py`.
matplotlib>=3.6
PySide6>=6.5
# Pillow 11.3+ provides Windows wheels for current Python releases, including
# Python 3.14; older versions fall back to a source build on Python 3.14.
Pillow>=11.3
pytest==7.4.0
pytest-forked>=1.6
pyinstaller>=6.15
pywebview>=5.0

# Notes:
# - `pywebview` is required for the active v3 HTML GUI under `python/gui_web/`
#   (launched via `run_web_gui.py` / `run_web_gui.bat`).
# - `PySide6` is required for the archived v2 Qt GUI under
#   `archive/legacy-ui/v2/python/gui_qt/`.
# - `customtkinter` is used by the archived v1 UI under
#   `archive/legacy-ui/v1/python/gui/`.
# - `pytest-forked` runs each test in its own subprocess via `--forked`.
#   Needed specifically for the archived v2 Qt UI tests: PySide6 has a
#   known segfault-on-interpreter-exit issue under a headless/offscreen
#   display (confirmed while setting up CI - see .github/workflows/tests.yml).
#   Forking isolates that crash to one subprocess instead of taking down
#   the whole test run and reporting a false failure.
# - If you only run v3, you can omit PySide6 and customtkinter.